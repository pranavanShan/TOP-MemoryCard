import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'

// import './index.css' for syling removed all default files to readd with ebtter names n stuff
// use this for testing (should prob use TDD a little more also actually use linter)

createRoot(document.getElementById('root')).render(
  <StrictMode>
  </StrictMode>,
)
